# system-admin

System inventory and recursive configuration drift analysis live in ../labtools.py; use inventory and drift commands.

See the repository root README for runnable commands, sample output, tests and limits.
