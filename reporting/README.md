# reporting

HTML escaping and parameterized in-memory SQLite reporting live in ../labtools.py; use html and sql commands.

See the repository root README for runnable commands, sample output, tests and limits.
