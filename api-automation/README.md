# api-automation

REST requests, bounded async aiohttp client and read-only Graph pagination live in clients.py; invoke through ../lab_cli.py.

See the repository root README for runnable commands, sample output, tests and limits.
